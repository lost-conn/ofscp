{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ofscp/schemas/v0.1/ws/ping.json",
  "title": "OFSCP WS ping (v0.1)",
  "description": "Liveness heartbeat. Either party MAY send `ping`; the peer MUST reply with `pong` echoing the ping's id in correlationId.",
  "allOf": [
    { "$ref": "./envelope.json" },
    {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": { "const": "ping" }
      }
    }
  ]
}
