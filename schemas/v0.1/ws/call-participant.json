{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ofscp/schemas/v0.1/ws/call-participant.json",
  "title": "OFSCP WS call.participant event (v0.1)",
  "allOf": [
    { "$ref": "./envelope.json" },
    {
      "type": "object",
      "required": ["type", "data"],
      "properties": {
        "type": { "const": "call.participant" },
        "data": {
          "type": "object",
          "additionalProperties": true,
          "required": ["groupId", "channelId", "user", "state"],
          "properties": {
            "groupId": { "type": "string", "minLength": 1 },
            "channelId": { "type": "string", "minLength": 1 },
            "user": { "$ref": "../defs/identity.json#/$defs/UserRef" },
            "state": { "type": "string", "enum": ["joined", "left"] }
          }
        }
      }
    }
  ]
}
