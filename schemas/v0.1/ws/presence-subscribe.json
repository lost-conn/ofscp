{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ofscp/schemas/v0.1/ws/presence-subscribe.json",
  "title": "OFSCP WS presence.subscribe command (v0.1)",
  "allOf": [
    { "$ref": "./envelope.json" },
    {
      "type": "object",
      "required": ["type", "data"],
      "properties": {
        "type": { "const": "presence.subscribe" },
        "data": {
          "type": "object",
          "additionalProperties": true,
          "required": ["users"],
          "properties": {
            "users": {
              "type": "array",
              "minItems": 1,
              "items": { "$ref": "../defs/identity.json#/$defs/UserRef" },
              "description": "Users whose presence the client wants. The provider applies the §6.1 presence visibility policy per subject."
            }
          }
        }
      }
    }
  ]
}
