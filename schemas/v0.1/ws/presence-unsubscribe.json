{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.invalid/ofscp/schemas/v0.1/ws/presence-unsubscribe.json",
  "title": "OFSCP WS presence.unsubscribe command (v0.1)",
  "allOf": [
    { "$ref": "./envelope.json" },
    {
      "type": "object",
      "required": ["type", "data"],
      "properties": {
        "type": { "const": "presence.unsubscribe" },
        "data": {
          "type": "object",
          "additionalProperties": true,
          "required": ["users"],
          "properties": {
            "users": {
              "type": "array",
              "minItems": 1,
              "items": { "$ref": "../defs/identity.json#/$defs/UserRef" }
            }
          }
        }
      }
    }
  ]
}
